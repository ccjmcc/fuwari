# 写文章指南（Fuwari）

## 1. 新建草稿

在项目根目录执行：

```bash
npm run new-post -- 你的文章文件名
```

示例：

```bash
npm run new-post -- cloudflare-ddns-实战
```

会生成：`src/content/posts/cloudflare-ddns-实战.md`

## 2. 必填 Frontmatter

新文件里会自动带这些字段：

```yaml
---
title: 文章标题
published: 2026-03-21T10:00:00
updated: 2026-03-21T10:00:00
description: 一句话摘要（建议 60-120 字）
image: ''
tags: []
draft: true
lang: 'zh_CN'
---
```

说明：

- `description`：会用于搜索摘要和社交卡片，尽量写清楚“问题 + 结论”。
- `image`：建议写封面图，利于分享展示（Open Graph / Twitter Card）。
- `tags`：建议 2-5 个，使用明确关键词（如 `["Cloudflare", "DDNS", "Linux"]`）。
- `draft: true`：写作中先隐藏；准备发布时改成 `false`。

## 3. 图片怎么放

1. 把图片放到 `src/content/assets/images/`
2. 在文章里使用相对路径：

```md
![封面](../assets/images/your-cover.png)
```

3. Frontmatter 的 `image` 也写同一路径：

```yaml
image: '../assets/images/your-cover.png'
```

## 4. 本地预览

```bash
npm run dev
```

打开 `http://localhost:4321` 检查：

- 标题、摘要、封面是否正常
- 文章链接是否可访问
- 手机和桌面排版是否正常

## 5. 发布前检查（SEO）

- `title`：包含核心关键词，避免过长
- `description`：自然可读，别堆关键词
- `tags`：与正文强相关
- `image`：有封面图
- `draft`：确认改为 `false`

## 6. 构建与发布

```bash
npm run build
git add .
git commit -m "feat: publish new post"
git push origin main
```
