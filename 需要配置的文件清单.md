# 需要配置的文件清单

## 📝 核心配置文件（必须修改）

### 1. `src/config.ts` - 主要配置文件
这是最重要的配置文件，包含以下配置项：

#### 网站基本信息
- `siteConfig.title` - 网站标题
- `siteConfig.subtitle` - 网站副标题
- `siteConfig.description` - 网站描述
- `siteConfig.keywords` - SEO关键词
- `siteConfig.lang` - 网站语言（'en', 'zh_CN', 'zh_TW', 'ja', 'ko', 'es', 'th'）

#### 主题颜色
- `siteConfig.themeColor.hue` - 主题色调（0-360）
- `siteConfig.themeColor.fixed` - 是否固定颜色
- `siteConfig.themeColor.forceDarkMode` - 是否强制暗色模式

#### 横幅图片
- `siteConfig.banner.enable` - 是否启用横幅
- `siteConfig.banner.src` - 横幅图片路径
- `siteConfig.banner.position` - 图片位置（'top', 'center', 'bottom'）
- `siteConfig.banner.credit` - 图片版权信息

#### 背景图片
- `siteConfig.background.enable` - 是否启用背景图
- `siteConfig.background.src` - 背景图片URL
- `siteConfig.background.position` - 背景位置
- `siteConfig.background.size` - 背景大小
- `siteConfig.background.repeat` - 背景重复方式
- `siteConfig.background.attachment` - 背景附着方式
- `siteConfig.background.opacity` - 背景透明度

#### 目录设置
- `siteConfig.toc.enable` - 是否显示目录
- `siteConfig.toc.depth` - 目录深度（1-3）

#### 网站图标
- `siteConfig.favicon` - 网站图标数组

#### 官方站点链接
- `siteConfig.officialSites` - 官方站点列表

#### 服务器链接
- `siteConfig.server` - 服务器链接列表

#### 导航栏配置
- `navBarConfig.links` - 导航栏链接列表

#### 个人资料配置
- `profileConfig.avatar` - 头像URL
- `profileConfig.name` - 姓名
- `profileConfig.bio` - 个人简介
- `profileConfig.links` - 社交链接（QQ、Telegram、Bilibili、GitHub等）

#### 许可证配置
- `licenseConfig.enable` - 是否启用许可证
- `licenseConfig.name` - 许可证名称
- `licenseConfig.url` - 许可证URL

#### 图片回退配置
- `imageFallbackConfig.enable` - 是否启用图片回退
- `imageFallbackConfig.originalDomain` - 原始域名
- `imageFallbackConfig.fallbackDomain` - 回退域名

#### Umami统计配置
- `umamiConfig.enable` - 是否启用Umami统计
- `umamiConfig.baseUrl` - Umami基础URL
- `umamiConfig.shareId` - Umami分享ID
- `umamiConfig.timezone` - 时区

#### 代码高亮配置
- `expressiveCodeConfig.theme` - 代码主题（如："github-dark"）

#### GitHub编辑配置
- `gitHubEditConfig.enable` - 是否启用GitHub编辑链接
- `gitHubEditConfig.baseUrl` - GitHub仓库URL

---

### 2. `astro.config.mjs` - Astro配置文件
- `site` - 网站URL（第37行）
- `base` - 基础路径（第38行）
- `redirects` - 重定向规则（第41-43行）

---

### 3. `package.json` - 项目依赖配置
- `name` - 项目名称
- `version` - 版本号

---

## 📁 数据文件（可选修改）

### 4. `src/data/friends/` - 友链数据
目录下包含所有友链的JSON文件，每个文件格式如下：
```json
{
  "name": "友链名称",
  "avatar": "头像URL",
  "description": "描述",
  "url": "链接地址"
}
```
**注意**：如果你想保留友链，可以保留这些文件；如果想清空，可以删除整个目录下的文件。

---

### 5. `src/data/sponsors/` - 赞助者数据
目录下包含所有赞助者的JSON文件，每个文件格式如下：
```json
{
  "name": "赞助者名称",
  "avatar": "头像URL",
  "date": "日期",
  "amount": "金额"
}
```
**注意**：如果你想保留赞助者信息，可以保留这些文件；如果想清空，可以删除整个目录下的文件。

---

## 🎨 样式和主题文件（可选修改）

### 6. `src/styles/` - 样式文件目录
- `main.css` - 主样式文件
- `markdown.css` - Markdown样式
- `variables.styl` - 样式变量定义

---

## 📄 其他配置文件（一般不需要修改）

### 7. `tailwind.config.cjs` - Tailwind CSS配置
一般不需要修改，除非需要自定义Tailwind主题。

### 8. `tsconfig.json` - TypeScript配置
一般不需要修改。

### 9. `svelte.config.js` - Svelte配置
一般不需要修改。

### 10. `postcss.config.mjs` - PostCSS配置
一般不需要修改。

### 11. `wrangler.jsonc` - Cloudflare Workers配置
如果使用Cloudflare部署，可能需要修改。

---

## 🖼️ 静态资源（保留）

以下目录和文件已保留，不需要修改：
- `public/` - 所有静态资源（图标、图片等）
- `src/content/assets/images/` - 文章图片资源
- `src/content/img/` - 图片资源

---

## ✅ 配置完成后的检查清单

1. ✅ 修改 `src/config.ts` 中的所有个人信息和网站信息
2. ✅ 修改 `astro.config.mjs` 中的网站URL
3. ✅ 检查并更新友链数据（`src/data/friends/`）
4. ✅ 检查并更新赞助者数据（`src/data/sponsors/`）
5. ✅ 运行 `pnpm install` 安装依赖
6. ✅ 运行 `pnpm dev` 测试本地开发
7. ✅ 运行 `pnpm build` 构建生产版本

---

## 📝 注意事项

- **所有文章已删除**：`src/content/posts/` 目录下的所有 `.md` 文件已清空
- **图片资源已保留**：所有图片文件都保留在 `src/content/assets/images/` 和 `src/content/img/` 目录
- **配置文件已保留**：所有主题配置、样式配置都已保留
- **友链和赞助者数据**：根据你的需求决定是否保留或清空
